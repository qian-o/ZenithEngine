using Graphics.Vulkan;
using Tests.AndroidApp.Controls;

namespace Tests.AndroidApp.Samples;

internal class RayQuery : ISample
{
    public void Load(Swapchain swapchain)
    {
    }

    public void Update(Swapchain swapchain, float width, float height, CameraController camera, float deltaTime, float totalTime)
    {
    }

    public void Render(CommandList commandList, Swapchain swapchain, float deltaTime, float totalTime)
    {
    }

    public void Unload()
    {
    }
}
